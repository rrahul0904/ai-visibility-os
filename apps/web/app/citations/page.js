import { AppShell, PageHeader } from "../../components/shell";
import { getStore } from "../../lib/store";
export default async function CitationsPage(){ const store=await getStore(); const rows=await store.listCitations(); return <AppShell><PageHeader eyebrow="Citation intelligence" title="Source influence graph" description="See which domains repeatedly shape AI answers and where your owned domain is missing."/><div className="table-wrap"><table><thead><tr><th>Domain</th><th>Provider</th><th>Prompt</th><th>Ownership</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><strong>{r.domain}</strong><div className="muted">{r.url}</div></td><td>{r.provider}</td><td>{r.promptId}</td><td><span className="pill">{r.isOwned ? "owned" : "external"}</span></td></tr>)}</tbody></table></div></AppShell> }
