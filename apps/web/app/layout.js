import "./globals.css";

export const metadata = {
  title: "Visibility OS",
  description: "AI discovery visibility intelligence and execution"
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
