import { AppShell, PageHeader } from "../../components/shell";
export default function SettingsPage(){ return <AppShell><PageHeader eyebrow="Workspace" title="Integrations & controls" description="Provider execution, search data, CMS publishing, crawler tracking and API access all sit behind explicit adapters and scoped credentials."/><div className="grid metrics">{[["AI providers","ChatGPT · Gemini · Perplexity"],["CMS publishing","WordPress · Webflow · Ghost · Shopify · Notion · Webhook"],["Crawler analytics","Server-side event SDK + verification"],["API / MCP","Scoped REST keys and agent-readable evidence"]].map(([a,b])=><div className="card" key={a}><h2>{a}</h2><p className="muted">{b}</p><span className="pill">adapter-ready</span></div>)}</div></AppShell> }
